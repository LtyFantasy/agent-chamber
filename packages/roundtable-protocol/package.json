{
  "name": "@agent-chamber/roundtable-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "圆桌模式共享契约包 —— 控制面信封/消息 payload/座位契约/注入消息体（纯类型 + 手写校验，零运行时依赖）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.12.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
